import { BuffaloJs } from '../../helpers';

export default class Contact extends BuffaloJs.Component {
  render() {
    return `<div>
      <p>Welcome to Contact, this is an experimental project</p>
      <a href="/">Go to Home</a>
    </div> `;
  }
}
